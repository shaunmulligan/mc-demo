version: '2'
services:
  websocket:
    build: ./websocket
    ports:
      - "80:80"
  wpe:
    build: ./wpe
    depends_on:
      - websocket
    privileged: true
    environment:
      WPE_URL: http://websocket:80/
