version: '2'
services:
  websocket:
    build: ./websocket
    ports:
      - "8080:8080"
  data2:
    build: ./data2
    ports:
      - "8081:8081"
  wpe:
    build: ./wpe
    depends_on:
      - websocket
    privileged: true
    environment:
      WPE_URL: http://websocket:80/
