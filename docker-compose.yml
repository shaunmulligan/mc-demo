version: '2'
services:
  proxy:
    build: ./haproxy
    ports:
      - "80:80"
  data1:
    build: ./data1
    ports:
      - "8080:8080"
  data2:
    build: ./data2
    ports:
      - "8081:8081"
  # wpe:
  #   build: ./wpe
  #   depends_on:
  #     - websocket
  #   privileged: true
  #   environment:
  #     WPE_URL: http://haproxy:8080/
